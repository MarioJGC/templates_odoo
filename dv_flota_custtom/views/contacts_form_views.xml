<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="partner_view_form" model="ir.ui.view">
        <field name="name">partner_view_form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='plate_number']" position="after">
                <field name="model_id" string="Modelo" required="1"/>
                <field name="model_year" string="Año" required="1"/>
                <field name="color" string="Color" required="1"/>
                <field name="nro_motor" string="Nro motor" required="1"/>
            </xpath>

            <xpath expr="//notebook[@colspan='4']" position="before">
                <button name="action_open_url"
                        type="object"
                        class="btn btn-secondary"
                        string="Consulta Vehicular">
                </button>
            </xpath>
        </field>
    </record>

</odoo>
